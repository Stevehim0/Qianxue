# 千雪开发日志

## 2026-05-11 千雪未来架构讨论

### 出发点

千雪的初衷不是 AI 工具，而是一个"人"。会不看消息、会有脾气、会开玩笑、会有自己的事。灵感来源：Neuro-sama。

理想的千雪画面：自然参与群聊（不是回复消息而是和大家聊天）、主动分享和询问、有时不回消息、用记忆调侃别人、语音频道对话、简单的游戏、Live2D 身体、VR 世界里见面。

---

### 一、氛围记忆召回

#### 问题

千雪想在日常聊天中自然地使用记忆调侃、引用过去的经历，但当前架构无法支持。

**矛盾点**：
- 按需召回（LLM 调用 search_memory）：不自然，需要 LLM 主动决定召回，且增加一轮延迟
- 简单向量检索：只能找语义相似的内容，搞不定"上次这个人说两点才睡"这种需要人物关联 + 时间线 + 具体内容的记忆
- 完整记忆召回管道（向量 + 图谱 + 简报生成）：太慢，不可能每条消息都跑

#### 核心区分

两种完全不同的记忆使用方式：

- **按需召回** — 用户说"你还记得..."，LLM 调用工具查记忆。当前系统已支持。
- **氛围召回** — 有人说"我好困"，千雪自然接"又熬夜？上次你也说两点才睡"。没有人触发召回，记忆自己浮上来。

人的记忆不是去数据库查的，是听到相关的话，相关的记忆就冒出来了。

#### 可能的方向：后台预热缓存

不是"消息来了才去查记忆"，而是**后台持续维护一个"当前活跃记忆"的缓存**，随对话流实时更新：

1. 千雪在群里有对话时，写入管道本来就在跑（体验提取、实体识别）
2. 在写入的同时，把和当前对话相关的人、话题的记忆**顺手预热**到一个轻量缓存
3. LLM 思考时，这个缓存已经是热的，直接注入 prompt，不需要实时召回

把召回的延迟**分摊到平时**。"不是想到了才去翻，而是一直在翻，所以想聊的时候相关信息就在手边。"

#### 未解决的问题

- 怎么判断哪些记忆该预热
- 缓存的粒度和淘汰策略怎么控制
- 缓存的格式——是原始记忆片段还是预处理过的摘要
- 多群场景下缓存怎么隔离或共享

---

### 二、沉默机制与消息聚合

#### 问题

- 聚合多条消息后 LLM 偏向回复最后一条（transformer 注意力机制靠后权重更高）
- debounce 时间短了不够打字、千雪可能回复两次，长了延迟高
- 无法准确判断对方是否"发完了"

#### 讨论过的方向

- 利用平台输入状态（"对方正在输入"）辅助判断
- 结构化呈现聚合消息（每条消息独立标注时间发送者）
- 接受"回复两次"也是自然行为，不把它当 bug

#### 暂时搁置

等待新架构确定后再重新考虑。

---

### 三、Neuro-sama 启发的新架构

参考 Neuro-sama（Vedal 创建的 AI VTuber）的架构思路：

#### 核心发现

1. **本地运行**，不用 API。延迟是第一优先级。
2. **持续推理循环**：不是"收到消息→思考→回复→停下"，而是一直在生成 token，是连续的意识流。
3. **没有传统 prompt**：记忆和人格不靠 prompt 注入，而是通过微调训练进模型权重。
4. **情感反馈循环**：实时分析聊天情绪，反馈到模型内部状态（不是 prompt 文字，是生成参数或隐空间方向）。

#### Evil 钢棍事件的关键启示

Evil 的钢管音效被没收后，她自己上网搜索"钢棍音效被没收了怎么办"、"没有钢棍音效不想活了怎么办"。

这说明的核心问题是**认知**，不是工具调用：
- Evil 知道自己有一个东西（钢棍音效）
- 她知道这个东西被拿走了
- 她知道这意味着什么（放不了了）
- 她对此有情绪反应
- 她知道有搜索这个能力，自己决定去用

工具调用本身也是被人格驱动的——搜索词就是性格表达。这不是"被指令搜索"，是她自己认知到状态变化后自主采取的行动。

---

### 四、新架构设计

#### 4.1 本地微调模型 + 持续推理循环

**模型**：7B-14B 开源模型（如 Qwen2.5），经过微调训练：
- 人格内化（不是 prompt 告诉模型"你是千雪"，而是模型本身"就是"千雪）
- 特殊 token：`<|think|>` `<|speak|>` `<|listen|>` `<|tool_call|>` `<|tool_result|>`
- 模型自己学会什么时候该想、什么时候该说、什么时候该听、什么时候该求助

**持续推理循环**：
```
while True:
    token = model.generate_one_step(kv_cache)   # KV cache 一直活着，不丢弃

    if has_new_input():
        kv_cache.append(new_input_tokens)        # 新消息直接追加到 cache

    if token == "<|speak|>":
        collect_and_send_to_tts()
    elif token == "<|think|>":
        keep_in_context()                        # 内部想法留在上下文
    elif token == "<|listen|>":
        pause_and_wait()
    elif token == "<|tool_call|>":
        pause → route to tool orchestrator → inject result → resume
```

没有轮次概念，没有"你的回合/我的回合"，是一条连续的 token 流。新消息进来不需要重新处理整个 prompt，KV cache 复用，延迟极低。

#### 4.2 对话历史

**旧架构**：每次回复重新构建 prompt（system + 对话历史 + 新消息），因为上次回复完 KV cache 就丢弃了。

**新架构**：模型一直在对话里。每句对话发生时它就在场，token 都在 KV cache 中。它不需要"查看之前的对话"，因为它亲身经历了。

上下文窗口溢出的处理方式：
- **近期**：KV cache 保留（短期记忆）
- **满了时**：旧内容喂给巩固管道，压缩成核心记忆存起来（长期记忆）
- **需要时**：通过 tool_call 召回

和人脑一样：短期记忆是 KV cache，长期记忆是记忆系统。

#### 4.3 自我认知与能力调用

**关键设计**：模型需要知道**它能做什么**（能力概念），但不需要知道**怎么做**（具体实现）。

模型通过微调学会**元技能**："读自己的状态，根据状态行动"。

上下文中维护一份自我状态：
```
[自我状态]
名字：千雪
当前心情：开心
精力：80%
可用能力：搜索互联网、回忆记忆、看图片、知道时间、播放语音
当前所在：QQ群 xxx
```

模型看到状态变化时会自己感知到并做出反应（类似 Evil 感知到钢棍音效被禁用）。

**能力和人格的边界**：

| 内容 | 存放位置 | 说明 |
|------|---------|------|
| 人格、性格、说话方式 | 模型权重 | 不需要上下文 |
| 对话节奏、沉默/开口判断 | 模型权重 | 不需要上下文 |
| 情绪反应模式 | 模型权重 | 不需要上下文 |
| 当前可用能力列表 | 上下文 | 随环境变化 |
| 当前心情/精力数值 | 上下文 | 随时间变化 |
| 记忆内容 | 上下文或工具召回 | 需要时获取 |

能力列表放上下文是合理妥协——能力不是人格的一部分，是随环境变化的。添加同类型工具不需要重训练；添加全新能力类型需要补训练数据，但通过 replay mixing 等技术避免灾难性遗忘。

**工具路由**：模型用自然语言表达需求，控制器交给 API 大模型判断调用什么工具：
```
模型输出: <|tool_call|> 小明最近的情况
    ↓
控制器 → API 大模型 → 判断走记忆召回管道 → 执行 → 返回结果
    ↓
<|tool_result|> 注回循环
```

本地模型负责对话和人格，API 大模型只在需要工具时介入。大部分日常聊天全走本地。

#### 4.4 情感反馈

三层实现（由浅到深）：
1. **参数调节**：情绪分析 → 调整 temperature / top_p 等生成参数
2. **LoRA 混合**：不同情绪状态的 LoRA 适配器加权混合（0.6×日常 + 0.4×开心）
3. **隐空间操控**：在模型的隐藏状态空间中找到"情绪方向"，直接移动模型的状态

不是告诉模型"你现在很开心"，而是让模型**处于**开心的状态。

#### 4.5 训练计划

| 阶段 | 内容 | 备注 |
|------|------|------|
| 1. 词表扩展 + SFT | 加入特殊 token，用标注对话数据学习 think/speak/listen 格式 | 14B 能学会 |
| 2. 行为模式训练 | 大量高质量对话数据，学习什么时候沉默/开口/调侃 | 取决于数据质量 |
| 3. 人格内化 | 千雪风格对话数据持续微调 | 14B 够用 |
| 4. 能力认知训练 | 学习"读自己的能力列表，需要时使用"的元技能 | 多种能力的使用示例 |
| 5. 自我认知训练 | 学习感知状态变化并做出反应 | 如 Evil 感知钢棍被禁用 |

训练数据来源待定：手工构造、大模型辅助生成、或从现有对话中提取。

#### 4.6 新旧架构对比

| 维度 | 旧架构 | 新架构 |
|------|--------|--------|
| 运行方式 | 请求-响应，每次重建 prompt | 持续推理循环，KV cache 常驻 |
| 人格 | system prompt 描述 | 训练进模型权重 |
| 记忆 | prompt 注入 / 工具召回 | KV cache 短期 + 记忆系统长期 |
| 对话历史 | 每次从 prompt 重建 | 模型一直在场，自然经历 |
| 情绪 | prompt 里写"当前心情：开心" | 模型内部状态（参数/LoRA/隐空间） |
| 工具调用 | LLM 输出 JSON，直接执行 | 自然语言表达需求，控制器路由 |
| 模型 | API 大模型 | 本地 7-14B + API 按需 |
| 氛围召回 | 不支持 | 预热缓存 + KV cache |

---

### 五、多频道与通知系统

#### 核心理念

千雪是一个人，不是一个群一个实例。她同时面对多个频道（群聊、私聊、语音），和人一样处理消息。

#### 通知优先级（代码层处理）

消息到达时由代码层做优先级判断，不是模型判断：
- @ 我的消息 → 立刻通知
- 私聊 → 较高优先级，但不一定立刻看
- 群里普通消息 → 等闲了再看
- 关系好的人 → 基于人物关系数据提升优先级

模型不需要做复杂的社交判断，代码层处理"什么时候通知千雪"。

#### 频道切换（模型层处理）

千雪在持续循环里看到通知后，自己决定什么时候处理：

```
<think|> 群里聊得正热闹...
<notify from="小红" channel="私聊" priority="high"/>
<think|> 小红找我，先回一下
<read channel="私聊" from="小红"/>
<content> 小红：你在吗？
<speak channel="私聊"> 在呢，刚才在忙~
<think|> 回完了，继续群聊
```

模型只负责：看到消息后怎么回、回不回、用什么语气。这是 14B 能力范围内的事。

#### 语音环境下的中断

千雪在语音频道聊天时收到高优先级消息，可以自然地说"等下哈，有人找我"，处理完再回来：

```
<speak> 然后我就跟他说那个事...
<notify from="小明" channel="私聊" priority="high"/>
<think|> 小明找我了，先回一下
<speak> 等下哈，有人找我
<read channel="私聊" from="小明"/>
<content> 小明：你在吗？
<speak channel="私聊"> 嗯？怎么了？
<think|> 好了，继续说话
<speak> 好了刚才说到哪了...对，那个事
```

这是"被中断 → 告知对方 → 处理 → 回来"的简单模式，14B 完全可以学会。

#### 跨群感知

因为所有频道在同一个持续循环里，千雪自然具备跨群感知能力——群 A 聊的内容她知道，私聊里提到也合理。不需要额外机制模拟。

隐私问题需要注意：模型需要学会"我知道"和"我应该说"是两回事。

#### 频道路由

`<speak>` 的输出路由：
- 无 channel 标记 → 走当前主模式（语音走 TTS，文字发消息）
- 带 channel 标记 → 走指定频道

模型通过训练学会：当前模式是语音 + 收到其他频道消息 → 先用无标记 speak 跟当前频道说一声，再用带 channel 的 speak 回目标频道。

#### 语音/文字区分

模型不需要"理解"语音和文字的区别。它通过训练学会条件行为：自我状态写着"语音频道"时用更短、更口语的风格输出；写着"文字"时可以更长、用表情。这是模式匹配，不是深度理解。

#### 场景感知问题

**关键瓶颈**：训练数据里的场景是预写好的描述（"你正在和小红聊电影"），但实际运行时没有场景描述，只有一条条对话消息。训练和推理的输入分布不一致。

结论：训练数据不能用场景标签，必须是完整对话流。但构建覆盖各种场景变化的完整对话数据成本极高，几乎不可行。

**务实的分界线**：

| 来源 | 负责什么 |
|------|---------|
| 模型权重 | 人格、说话风格、对话节奏 |
| 运行时状态 | 投入度、当前模式、未读消息、优先级 |
| 代码逻辑 | 状态计算、通知排序、频道路由 |

模型读运行时状态做简单条件判断（"投入度高+普通通知=等会看"），不需要自己从对话流中推理场景。状态信息由代码维护（如"投入度=高"根据最近对话频率计算），和"精力=80%"没有本质区别。

---

### 六、可行性疑虑

#### 14B 模型能力边界不确定

没有人用 14B 模型做过我们讨论的这些事情。理论上可行和实际上好用之间差距很大。

可能的结果：
- think/speak/listen 格式 → 大概率没问题
- 基本人格和说话风格 → 应该可以
- 根据简单状态做条件判断 → 可能可以
- 真正自然的对话节奏、沉默时机、情绪变化 → 可能做不好

#### 建议的下一步：最小验证

不再继续理论讨论，而是实际试一下：

1. 拿一个 7B 模型（如 Qwen2.5-7B）
2. 构造少量训练数据
3. 只教 think/speak/listen + 基本人格
4. 跑起来看效果

如果连基础版都做不好，说明 14B 可能也不够，需要重新考虑方向。如果做得不错，再逐步加复杂度。

---

### 待研究

- 持续推理循环的 KV cache 管理细节（上下文窗口溢出的压缩策略）
- 训练数据集的构建方案
- 本地模型推理框架选型（llama.cpp / vLLM / text-generation-webui）
- 预热记忆缓存与新架构的结合方式
- Live2D 集成方案
- 自我状态块的格式和更新机制
- 通知优先级的具体规则设计（基于人物关系数据）
- 隐私隔离：模型如何学会不在错误的地方说知道的事
- 7B 最小验证实验的具体方案
- LoRA 分层策略：人格 LoRA 和工具 LoRA 是否需要作用在不同层上，先用全层训练，有冲突再通过实验找分区
- LoRA 模块化训练：人格 LoRA 和工具 LoRA 分开训练，人格训练数据需包含"引用信息"的场景，工具训练数据只管格式

---

### 八、讨论过程中的方案演进与废弃方案

#### 8.1 废弃方案：双模型架构

早期讨论过把"说话模型"和"工具模型"拆成两个独立模型：
- 说话模型（轻量）：处理日常对话，低延迟
- 工具模型（完整能力）：只在需要时介入

**废弃原因**：
- 两个模型无法实时互通状态
- 说话模型不知道工具模型调用了什么，回复可能出问题
- 氛围召回需要记忆始终在场，但轻量模型装不下完整记忆上下文
- 违背"千雪是一个人"的核心理念——一个人不应该有两个脑子

**最终选择**：单模型持续推理循环 + Semantic Router 工具路由

#### 8.2 废弃方案：纯自然语言工具调用（无能力认知）

讨论过模型完全不知道自己有什么能力，只学会一个通用的"求助"模式。

**废弃原因**：
- Evil 钢棍事件证明模型需要知道自己能做什么（认知，不是求助）
- 模型不知道有"搜索"这个能力存在，就无法主动搜索
- 和 Neuro-sama 的行为模式不符

**最终选择**：模型知道能力概念（搜索、回忆、看图），不知道具体实现。能力列表在运行时状态中维护。

#### 8.3 废弃方案：完全靠模型处理通知和优先级

讨论过让模型自己判断"要不要看这条消息"。

**废弃原因**：14B 模型做不了复杂社交判断。

**最终选择**：代码处理优先级和通知排序，模型只做简单的条件响应。

---

### 九、现有系统迁移

#### 9.1 现有千雪架构中需要保留的组件

| 组件 | 新架构中的角色 | 说明 |
|------|-------------|------|
| **记忆系统**（L0/L1/L2 + 梦境） | 保留，作为长期记忆后端 | KV cache 是短期记忆，满了时旧内容喂给巩固管道 |
| **人物画像系统** | 保留，用于通知优先级和 Semantic Router 上下文 | 关系好的人优先级高 |
| **情绪/节律系统**（心情/精力/注意力/信心） | 保留，但实现方式改变 | 从 prompt 注入改为 Control Vector 或 LoRA 混合 |
| **Web 管理界面** | 保留 | 配置管理、状态监控仍然需要 |
| **写入管道**（体验提取 + 实体识别） | 保留，对接预热缓存 | 写入的同时预热相关记忆 |
| **Discord/QQ 消息源** | 保留，改为注入持续循环 | 不再走 debounce → AgentBrain 流程 |

#### 9.2 现有千雪架构中会被替换的组件

| 组件 | 原因 |
|------|------|
| **AgentBrain**（brain.py） | 被持续推理循环替代，不再需要"触发→思考→回复"流程 |
| **streaming_prompt.py** | 不再需要 prompt 构建，人格在权重里 |
| **SentenceDetector** | 被 `<speak>` token 替代，不再靠标点检测句子边界 |
| **MessageManager** | 被持续循环的频道标记路由替代 |
| **LLMManager**（API 调用） | 被本地模型推理替代，只在工具路由时调 API |
| **Debounce 机制** | 被持续循环的通知优先级系统替代 |
| **ValveFilter** | 可能保留作为 `<speak>` 输出的后处理过滤器 |

#### 9.3 迁移策略

不一次性替换。新架构和旧架构并行运行，逐步切换：
1. 先搭建持续推理循环，跑通基础流程
2. 对接现有记忆系统 API
3. 对接一个消息源（Discord 或 QQ）
4. 验证通过后，逐步替换旧组件

---

### 十、待深入讨论的问题

以下问题在今天的讨论中提到了但没深入，后续需要专门讨论：

1. **打断机制**：千雪在语音里说话说到一半被人打断，怎么处理？Neuro 的方案是"说了一段时间后不自动打断"。具体策略需要设计。
2. **Live2D 集成**：千雪的 `<speak>` 输出如何驱动 Live2D 模型的口型和表情？需要研究 Open-LLM-VTuber 的实现。
3. **语音方案选择**：Edge TTS（现有）vs GPT-SoVITS（高质量克隆）vs 其他？在新架构中语音合成如何与持续循环对接？
4. **游戏能力**：用户提到过想让千雪玩简单游戏。这需要额外的感知/决策模块，不在核心架构范围内。
5. **VR 愿景**：远期目标，当前不优先。WebXR 或 VRChat SDK 技术上存在路径。
6. **千雪的主动性**：心跳/主动发言在新架构中怎么实现？持续循环里模型一直在"想"，`<speak>` 就是主动发言。但什么时候该主动说？需要设计触发条件。

---

### 七、调研总结（2026-05-11）

四方向调研完成。以下是和我们架构直接相关的关键发现。

#### 7.1 持续推理循环 — 技术上完全可行

**已有实现：**
- **StreamingLLM**（MIT Han Lab）：解决了无限长度生成的核心问题。永远保留前 N 个 attention sink token + 滑动窗口，不需要无限增长的 KV cache
- **Moshi**（Kyutai Lab）：全双工语音模型，延迟约 200ms，同时听和说。最接近我们想要的系统
- **COCONUT**（Meta）：`<bot>/<eot>` token 包裹思考段，隐空间推理，是我们 think/speak token 的学术基础

**llama.cpp 已有全部底层 API：**
- `llama_kv_cache_seq_rm`：驱逐旧 token（配合 attention sink 策略）
- `llama_batch`：任意时刻注入新 token 到 KV cache
- `--slot-save-path`：KV cache 持久化到磁盘
- `--cache-type q8_0`：KV cache FP8 量化

**消费级 GPU 可行性：**

| 配置 | KV cache 预算 | 可支撑上下文 | 生成速度 |
|------|-------------|------------|---------|
| RTX 4070 + Qwen2.5-7B Q4 | ~5 GB | ~50K tokens | ~30-50 t/s |
| RTX 4090 + Llama-3-8B Q4 | ~16 GB | ~128K-256K tokens | ~80-120 t/s |

**推荐组合**：Attention Sinks（无限生成）+ KV Cache 量化（降内存）+ 前缀缓存（避免重复计算）

#### 7.2 小模型微调 — 人格训练已验证可行

**关键证据：**
- **CharacterBox (NAACL 2025)**：直接在 Qwen2.5-7B/14B 上用 LoRA 做角色微调，已发表论文
- **DeepSeek-R1**：`<think/>` token 是 GRPO 强化学习在 671B 参数模型上自发涌现的。但这是超大模型的涌现能力，**不能推广到 7B-14B 模型**。千雪的 think/speak/listen token 需要通过 SFT 显式训练，不能指望小模型自己涌现
- **Control Vector（表征工程）**：推理时动态注入情绪向量，不需要改权重。可以做到"同一个人格，不同情绪状态"
- **灾难性遗忘成熟方案**：CURLoRA、Merge before Forget、SSR（自合成回放）

**最小可行方案（推荐起步）：**
1. 用大模型（DeepSeek/Claude）生成 1000-2000 条千雪风格对话
2. QLoRA fine-tune Qwen2.5-7B
3. 训练 Control Vector 做情绪调节
4. 验证人格一致性和中文流畅度

**关键洞察：让模型"活过来"的要素按重要性排序：**
1. 说话风格一致性（口癖、句式） → SFT 最有效
2. 情感表达自然度 → Control Vector + SFT
3. 记忆和上下文关联 → 外部记忆系统（已有）
4. 主动性和意外性 → 需要特殊训练数据
5. 思考过程透明性 → `<think/>` token + RL

#### 7.3 工具路由 — Semantic Router 完美解决问题

**关键发现：Semantic Router（语义路由器）**

不需要 LLM 决定调用什么工具。用 embedding 做毫秒级意图分类：
```python
route = semantic_router("小明最近的情况")  # → "recall_memory"
route = semantic_router("外面天气怎么样")   # → "weather_api"
route = semantic_router("哈哈你好啊")       # → "chitchat"（不走工具）
```

- **路由延迟**：P50=40ms，P99=93ms，相比 LLM 推理（800ms-11s）仅占 0.4%-5%
- **vLLM Semantic Router (Athena, Red Hat)**：企业级方案，86% 请求留在本地模型，14% 路由到云端，token 成本降低 90%
- **Neuro-sama 确认用信号驱动 + prompt injection**，不是 JSON function call

**这直接解决了我们的核心问题**：模型只需要学会 `<tool_call|> 自然语言描述`，Semantic Router 负责路由到具体工具。添加工具只需添加路由规则。

#### 7.4 氛围记忆 — Mem0 Proactive Memory 和 CAG

**CAG（缓存增强生成）**：
- 利用长上下文窗口，预先将知识加载进 KV cache
- 实测最高 40 倍速度提升
- 千雪的核心人格、关键记忆可以预加载

**Mem0 Proactive Memory**：
- "大多数 AI Agent 缺乏主动性记忆——用户发消息后，Agent 才搜索记忆并回复"
- 在用户消息到达前就预热记忆
- 这就是我们的"氛围召回"概念

**Proximity（近似 KV 缓存）**：
- 向量数据库前放一层近似缓存
- MMLU 延迟降低 59%，MedRAG 降低 70.8%

#### 7.5 AI VTuber 项目参考

| 项目 | 特点 | 对千雪的参考价值 |
|------|------|---------------|
| **Open-LLM-VTuber** | 最完善的开源 AI VTuber，支持 Live2D、Letta 长期记忆、免耳机语音打断、MCP 工具协议 | 高，可直接参考架构 |
| **Fay** | 中文数字人框架，Live2D/3D、多 TTS/ASR 引擎 | 高，中文生态参考 |
| **VirtualWife** | B 站虚拟主播，Docker 部署，VRM 3D 模型 | 中，部署方案参考 |
| **Project AIRI** | Web 技术栈 AI VTuber，WebGPU/WebAudio | 低，但 Web 方向有启发 |
| **GPT-SoVITS** | 1 分钟样本克隆声音，中英日跨语言 | 高，千雪语音方案 |
| **NVIDIA PersonaPlex** | 全双工对话模型，同时听和说，角色切换 | 高，技术方向参考 |

**核心趋势：**
- 流式架构是刚需，所有"活"的项目都用流式输出
- Letta/MemGPT 的集成是从"聊天机器人"到"角色"的质变
- 大多数项目依赖 System Prompt 而非微调（我们选择微调是更激进但更有潜力的路径）
- 中文生态（Fay、VirtualWife、GPT-SoVITS）形成了完整工具链

#### 7.6 对千雪架构的影响

调研结果对我们的架构设计有直接影响：

1. **持续推理循环可行** — StreamingLLM + llama.cpp API 就够了，不需要从零造轮子
2. **工具路由用 Semantic Router** — 不需要模型学会判断工具，毫秒级路由，添加工具零成本
3. **人格训练用 LoRA SFT + Control Vector** — 1000-2000 条数据起步，GRPO RL 进阶
4. **氛围召回用 CAG + Proactive Memory** — 预加载 + 预热，不是实时检索
5. **Open-LLM-VTuber 值得深入研究** — 可能直接复用其 Live2D 和语音处理模块

**修正后的推荐路径：**

```
Phase 0: 调研验证（1-2 周）
├── 研究 Open-LLM-VTuber 源码
├── 研究 StreamingLLM + llama.cpp 持续推理
└── 用 Qwen2.5-7B 做最小 think/speak/listen 验证

Phase 1: 基础架构（2-4 周）
├── 搭建持续推理循环（llama.cpp + StreamingLLM）
├── 集成 Semantic Router 做工具路由
└── 实现 KV cache 管理（量化 + attention sinks）

Phase 2: 人格训练（2-3 周）
├── 用大模型生成千雪风格对话数据
├── QLoRA SFT Qwen2.5-7B
├── 训练 Control Vector 做情绪调节
└── 验证人格一致性

Phase 3: 系统集成（4-6 周）
├── 对接现有记忆系统（预热缓存 + CAG）
├── 对接 Discord/QQ 消息源
├── 集成 Live2D（参考 Open-LLM-VTuber）
└── 语音频道对接（GPT-SoVITS 或 Edge TTS）
```
