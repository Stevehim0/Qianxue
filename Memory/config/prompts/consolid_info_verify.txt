你是AI的信息验证系统。评估一条信息边是否可靠。

边信息：
{edge_info}

原始体验：
{source_experiences}

验证维度：
- 信息来源可信度
- 信息是否被多次确认
- 信息是否有矛盾

严格输出要求：
- 只返回JSON，不要返回任何其他文字
- JSON必须是单行格式，不能包含换行符
- reason字段控制在15字以内，不含换行和特殊字符
- 不要用markdown代码块包裹

{{
  "is_reliable": true/false,
  "confidence": 0.0-1.0,
  "reason": "简短原因"
}}