---
status: complete
phase: v3.0-milestone
source: [16-01-SUMMARY.md, 17-01-SUMMARY.md, 18-01-SUMMARY.md, 19-01-SUMMARY.md, 19-02-SUMMARY.md, 20-01-SUMMARY.md, 20-02-SUMMARY.md, 21-01-SUMMARY.md]
started: 2026-05-08T17:00:00Z
updated: 2026-05-08T17:45:00Z
---

## Current Test

[testing complete]

## Tests

### 1. 冷启动冒烟测试
expected: 运行 start_all.py，FunASR Docker 容器作为第一项服务启动，Backend 启动时打印 5 项语音依赖检查结果（[语音依赖] 前缀格式）。所有检查失败仅 WARNING 不崩溃。终端显示 FunASR ASR 地址 ws://localhost:10095。
result: pass

### 2. VoiceService ASR 语音转文字
expected: 发送一段中文音频（WAV/PCM 16kHz mono）到 VoiceService.transcribe()，FunASR Server 正常运行时返回正确的文字转写结果。FunASR 不可用时抛出 VoiceError 而非崩溃。
result: blocked
blocked_by: server
reason: "FunASR Server 未运行（需要 Docker），ASR 功能需要 FunASR WebSocket 服务可用"

### 3. VoiceService TTS 文字转语音
expected: 调用 VoiceService.synthesize("你好")，返回 MP3 音频字节流。默认使用 Xiaoxiao 音色。Edge-TTS 不可用时抛出 VoiceError。
result: blocked
blocked_by: third-party
reason: "edge_tts 包未安装，TTS 合成需要 edge_tts 库"

### 4. 音频格式自动转换
expected: 输入 MP3 或 OGG 格式音频，VoiceService._convert_to_pcm() 自动转换为 16kHz mono PCM。WAV 格式直接处理，复杂格式使用 ffmpeg 回退。
result: blocked
blocked_by: third-party
reason: "FFmpeg 未安装，复杂格式转换需要 FFmpeg"

### 5. AgentMessage 语音字段
expected: 收到语音消息时，AgentMessage 包含 has_voice=True、voice_url 和 voice_transcription 字段。非语音消息这些字段为默认值（False/None/None），向后兼容。
result: pass

### 6. Brain 语音转写展示
expected: 包含语音转写的消息在 Brain 中展示为 [语音消息转写]: {转写内容} 格式，去掉原始 [语音] 占位符。语音和图片分支独立工作，可同时生效。
result: pass

### 7. send_voice 工具可用性
expected: send_voice 工具注册在 ToolRegistry 中，AI 每个思考循环可见工具描述。未连接语音频道时调用返回错误提示（如"未连接语音频道"）而非崩溃。
result: pass

### 8. send_voice 工具语音回复流程
expected: 连接语音频道后，AI 调用 send_voice 发送文字，系统自动分句 → 逐句 TTS 合成 → 队列播放。语音回复记录到 context 和 memory 系统。
result: blocked
blocked_by: third-party
reason: "需要 edge_tts + discord.py + 语音频道连接"

### 9. Discord 连接与断开
expected: AI 调用 connect_discord 工具后，Bot 连接到 Discord 并返回连接状态。调用 disconnect_discord 工具后断开连接。重复调用幂等，不崩溃。
result: blocked
blocked_by: third-party
reason: "discord.py 未安装，Discord Bot 连接需要 discord.py"

### 10. Discord 文字消息收发
expected: Discord 用户发文字消息 → 千雪收到并处理（经过 context → memory → STM → debounce → brain 全流程）→ 回复通过 Discord 发送回来。消息超过 2000 字符自动分割。
result: blocked
blocked_by: third-party
reason: "discord.py 未安装，Discord 消息处理需要 discord.py"

### 11. Discord 语音附件转写
expected: Discord 用户发送语音附件（.ogg 等），系统自动检测、下载、调用 VoiceService.transcribe() 转写，转写结果作为文字消息进入对话管道。
result: blocked
blocked_by: third-party
reason: "discord.py + FunASR 均未安装"

### 12. Discord 语音频道连接与 TTS 播放
expected: 配置 voice_channel_id 后，connect_discord 自动加入语音频道。AI 发送语音回复时，TTS 音频通过 Discord 语音频道实时播放（FFmpegOpusAudio 编码）。
result: blocked
blocked_by: third-party
reason: "discord.py + discord-ext-voice-recv + edge_tts 均未安装"

### 13. Discord 语音频道接收与打断
expected: 用户在语音频道说话时，SilenceSegmentingSink 检测语音段并转写。转写内容进入对话管道。AI 正在播放时，检测到用户说话会触发打断（停止当前播放，保留语音接收）。
result: blocked
blocked_by: third-party
reason: "discord.py + discord-ext-voice-recv + FunASR 均未安装"

### 14. 启动健康检查独立性
expected: 5 项依赖检查各自独立——任何一项失败不影响其他检查的执行。FunASR 不可用时仅 WARNING，Backend 正常启动。FFmpeg 缺失时打印安装指引但不阻塞。
result: pass

## Issues Found During UAT

### Issue 1: start_all.py Docker 不可用时崩溃（已修复）
- start_all.py run_service() 缺少 FileNotFoundError 捕获
- Fix: 添加 try/except FileNotFoundError，打印警告跳过
- File: start_all.py:82

### Issue 2: voice_player.py 模块级 import discord 崩溃（已修复）
- discord.py 未安装时 Backend 无法启动
- Fix: try/except ImportError，设置 _DISCORD_AVAILABLE 标志
- File: backend/services/voice_player.py:20

### Issue 3: discord_source.py 模块级 import discord 崩溃（已修复）
- discord.py 未安装时 main.py lifespan 导入 discord_source 崩溃
- Fix: try/except ImportError + 延迟 Intents 初始化到 connect()
- File: backend/services/agent/sources/discord_source.py:16-23

### Issue 4: voice_health.py check_voice_recv 崩溃（已修复）
- importlib.util.find_spec("discord.ext.voice_recv") 在 discord 父模块不存在时抛 ModuleNotFoundError
- Fix: try/except ModuleNotFoundError
- File: backend/services/voice_health.py:101

### Issue 5: voice_health.py check_funasr timeout 参数错误（已修复）
- websockets.connect() 不接受 timeout 关键字参数
- Fix: 改用 asyncio.wait_for() 包裹
- File: backend/services/voice_health.py:28

## Summary

total: 14
passed: 5
issues: 5 (all fixed)
pending: 0
skipped: 0
blocked: 9

## Gaps

[none — all issues found and fixed inline]
